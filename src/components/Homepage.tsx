const Homepage = () => {
  return (
    <div
      style={{
        display: "grid",
        justifyContent: "center",
      }}
    >
      <p>This is the homepage (Protected route).</p>
      <p>You can refresh the page</p>
    </div>
  );
};

export default Homepage;
